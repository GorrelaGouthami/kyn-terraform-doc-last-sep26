##lets install choco package manager
  #open powershell in admin mode inside the rdp
  Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
  ##once you install choco 
  refreshenv
  ###after this close the powershell start again in admin mode
  choco install git -y
  choco install vscode -y
  choco install terraform -y
  choco install azure-cli -y
  ###once installation complete close the power shell start again withought admin mode
terraform --version
  az --version
  git version
  ###this will verify that your package installation complete visual studio code will be there in the desktop
