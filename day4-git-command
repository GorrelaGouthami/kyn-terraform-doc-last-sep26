#lets first create the environment varaibles. 
  #before that we need to open gitbash
  #lets cresate a dir
 mkdir projectx
cd projectx/
  #inside the directory we will create some blank file
touch DS_Store
touch auth
touch secret
touch calcy.py
#lets configure some global variables 
git config --global user.name "gopal"
git config --global user.email "gopal1409@gmail.com"
git config --global --list
#to start maning this code with git you need to intialized and intilzation happen in current directory
git init
####now you can run status this will show that the code is managed by git
git status
#lets add a new line in calcy.py
echo "we are starting calcy application" > calcy.py
git status
git add calcy.py
git commit -m "started calcy application"
#lets add another line after our first commit
echo "inside calcy app we are adding a new function called as divide" >> calcy.py
cat calcy.py
#status will show that the calcy.py has been modified
git status
##git diff will show the content modified
git diff
#any changes you do in the code and want to be visualized in remote repo do add commit 
git add calcy.py
git commit -m "added divide function"
#git log will show all your commit so always give menaing message
 git log
#lets generate the ssh key from the github terminal
ssh-keygen -t rsa
###once you run this commannd do not change anything just press enter enter enter this will create the key 
##once create type cat 
cat pathofthepublickey you will find in the output of the key
##copy the whole key 
#go to github extreme right last icon at the top click on it seeting-click on it 
#access menu you will find ssh and gpg key click on it add ssh key give a name and paste it 
#we will add a new line 
echo "testing my calcy project" >> calcy.py
git status
git add calcy.py
git commit -m "testing my calcy project"
#any changes you make in your local repo you need to push it also 
git push origin main
#once push refresh the repo you will see the third line 
#####branching in git
#git branch will show we are in which branch and how many branches we have
  git branch
#currently we are in main branch and calcy.py have three line
  cat calcy.py
#lets create a new branch
git branch subtract
git branch
#to move to the new branch checkout 
git checkout subtract
#again git branch will show we are in which branch
git branch
#we will add a new line in subtract branch 
cat calcy.py
echo "adding subtract function" >> calcy.py
#now the subtract branch have 4th line 
cat calcy.py
git status
git add calcy.py
#do a commit in subtract branch 
git commit -m "added subtract function"
git branch
#if we move back to our main branch and check the file it has only three line 
git checkout main
cat calcy.py
git branch
##finally push this branch in github
git push origin subtract
git push origin --all
##once push go to github and check the same
#once you complete with the feature branch you need to merge it
#first you need to switch to the branch where you want to merge using checkout command
git branch
git checkout main
#run a merge sommand provide the branch name you want to merge
##it will open in vi editro #press esc :wq this will save the file 
git merge subtract
#now in the main branch if you see calcy.py it contains the 4th line 
cat calcy.py
#finally push the change to remote
git push origin --all
git reflog
###once push in the master branch check the calcy.py has the 4th line

